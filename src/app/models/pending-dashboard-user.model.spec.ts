import { PendingDashboardUser } from './pending-dashboard-user.model';

describe('PendingDashboardUser', () => {
  it('should create an instance', () => {
    expect(new PendingDashboardUser()).toBeTruthy();
  });
});
