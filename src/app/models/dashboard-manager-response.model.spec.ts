import { DashboardManagerResponse } from './dashboard-manager-response.model';

describe('DashboardManagerResponse', () => {
  it('should create an instance', () => {
    expect(new DashboardManagerResponse()).toBeTruthy();
  });
});
